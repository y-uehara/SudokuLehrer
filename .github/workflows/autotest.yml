name: CI

on: [push]

jobs:
  backend_test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: |
      cd backend/test
      mkdir build
      cd build
      ls
        

